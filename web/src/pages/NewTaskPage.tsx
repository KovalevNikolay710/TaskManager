import { StubPage } from './StubPage'

/** Экран создания задачи /tasks/new — заглушка до макета. */
export function NewTaskPage() {
  return <StubPage title="Новая задача" text="Здесь будет форма новой задачи с дедлайном и оценкой времени — макет экрана ещё не готов." />
}
